import styled from "styled-components"

export const DropdownSty = styled.div`
    display: flex;
    align-items: center;
    width: 86px;
    > :where(.css-dev-only-do-not-override-1vtf12y).ant-image .ant-image-img {
        width: 40px;
        height: 40px;
        border-radius: 50% !important;
    }
     .Avatar {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        cursor: pointer;
    }
      > :where(.css-dev-only-do-not-override-1vtf12y).ant-image-preview-root .ant-image-preview-img {
        max-height: 80% !important;
    }
   .rootClassName{
      transition: opacity 3s ease-in;
   }
  
    > .Login {
        margin-left: 10px;
        height: 100%;
        cursor: pointer;
    }
`
